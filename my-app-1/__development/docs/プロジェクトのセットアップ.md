# インストール

コマンド

```bash
npx create-next-app@latest my-app-16
```

実行結果ログ

```bash
Need to install the following packages:
create-next-app@16.0.1
Ok to proceed? (y) y
✔ Would you like to use the recommended Next.js defaults? › No, customize settings
✔ Would you like to use TypeScript? … No / *Yes
✔ Which linter would you like to use? › ESLint
✔ Would you like to use React Compiler? … *No / Yes
✔ Would you like to use Tailwind CSS? … No / *Yes
✔ Would you like your code inside a `src/` directory? … No / *Yes
✔ Would you like to use App Router? (recommended) … No / *Yes
✔ Would you like to use Turbopack? (recommended) … No / *Yes
✔ Would you like to customize the import alias (`@/*` by default)? … *No / Yes
Creating a new Next.js app in /workspaces/portfolio-nextjs/my-app-16.

Using npm.

Initializing project with template: app-tw 


Installing dependencies:
- react
- react-dom
- next

Installing devDependencies:
- typescript
- @types/node
- @types/react
- @types/react-dom
- @tailwindcss/postcss
- tailwindcss
- eslint
- eslint-config-next


added 366 packages, and audited 367 packages in 1m

145 packages are looking for funding
  run `npm fund` for details

found 0 vulnerabilities

Generating route types...
✓ Route types generated successfully

Success! Created my-app-16 at /workspaces/portfolio-nextjs/my-app-16
```

# プロジェクトのモジュール形式

package.json
```js
{
  "name": "my-app-16",
  "version": "0.1.0",
  "private": true,
  "type": "module",   // ★これを指定する
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    ...
}
```
* `"type": "module"` - プロジェクト全体を ESM(ESModules) で統一する。

tsconfig.json
```js
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",                 // ★これを指定する
    "moduleResolution": "bundler",      // ★これを指定する
    "resolveJsonModule": true,
    "isolatedModules": true,
    ...
}
```
* `"module": "esnext"` `"moduleResolution": "bundler"` - Next.js のデフォルト設定

# APIモック(Express)

Express を使った API モック。TypeScript / ESM 構文でコードを実装する。

### tsx を使ってモックを起動する場合

tsx をインストール
```bash
npm install -D tsx
```

 起動スクリプト
```js
// package.json
{
  "name": "my-app-16",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "mock1": "tsx watch __development/mocks/customers.ts"  // ★起動スクリプト
  },
  ...
}
```
* `watch` - ソースコードを監視し、ファイルが変更されるたびに自動で再実行するモード。

### ts-node/esm を使ってモックを起動する場合

ts-node をインストール
```bash
npm install -D ts-node
```
 起動スクリプト
```js
// package.json
{
  "name": "my-app-16",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "mock1": "tsx watch __development/mocks/customers.ts",
    "mock2": "node --no-warnings --loader ts-node/esm __development/mocks/users.mts",  // ★起動スクリプト
  },
  ...
}
```

### モックの起動
```bash
npm run mock1
```

### アプリケーション本体とAPIモックを一緒に起動できるようにする。

npm-run-all をインストール
```bash
npm install -D npm-run-all
```

起動スクリプト

```js
// package.json
{
  "name": "nextjs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "mock1": "tsx watch __development/mocks/customers.ts",
    "dev:mock": "npm-run-all --parallel dev mock1",  // ★起動スクリプト
  ...
}
```

起動コマンド
```bash
npm run dev:mock
```

# Next:Jest

Next.js 用の Jest 構成。Jest コードを TypeScript / ESM 構文で実装し、 ESM モードで Jest を実行する。

インストール
```bash
npm install -D jest @types/jest
```
```bash
npm install -D cross-env
```
* Jest は デフォルトでCJS(CommonJS) で動作するため、ESM で動作できるようにするために cross-env を使用する。


設定ファイルを生成する

```bash
npm init jest@latest
```

```bash
The following questions will help Jest to create a suitable configuration for your project

✔ Would you like to use Jest when running "test" script in "package.json"? … no
✔ Would you like to use Typescript for the configuration file? … yes
✔ Choose the test environment that will be used for testing › node
✔ Do you want Jest to add coverage reports? … yes
✔ Which provider should be used to instrument code for coverage? › v8
✔ Automatically clear mock calls, instances, contexts and results before every test? … yes

📝  Configuration file created at /workspaces/portfolio-nextjs-2/my-app-1/jest.config.ts
```

設定ファイルを `next/jest` を使用する構成として変更する。

```ts
// jest.config.ts
import nextJest from 'next/jest.js';

const createJestConfig = nextJest({
	dir: './',
});

const config = {
	clearMocks: true,
	collectCoverage: true,
	coverageDirectory: "coverage",
	coverageProvider: "v8",
	moduleNameMapper: {
		'^@/(.*)$': '<rootDir>/$1',
	},
	testEnvironment: "jest-environment-node",
	testMatch: ['**/__development/tests/next-jest/**/*.?([mc])[jt]s?(x)'],
	extensionsToTreatAsEsm: ['.ts', '.tsx'],
};

export default createJestConfig(config);
```
* `extensionsToTreatAsEsm: ['.ts', '.tsx']` - Jest を ESM モードで実行するため、.ts を ESM として扱う設定。

tsconfig.json
```js
{
  "compilerOptions": {
    ...
    "types": ["jest"],						// ★これを追加
    "paths": {
      "@/*": ["./src/*"],
      "@/tests/*": ["./__development/tests/*"]	// ★これを追加（パス解決の設定）
    }
  },
  ...
}
```
* tsconfig.json は、VSCode の補完 / 型チェック(静的インポート)で使用される

```ts
// jest.config.ts
...
const config = {
	...
	moduleNameMapper: {
		'^@/(.*)$': '<rootDir>/src/$1',	// ★これを記述
	},
	...
};
...
```
* jest.config.ts は jest 実行時のパス解決で使用される（動的インポートや、モジュールのモックなど）

Jest 実行

```bash
npm exec -- cross-env NODE_OPTIONS=--experimental-vm-modules jest [テストファイル名] -t [テストメソッド名]

# コマンド例
# npm exec -- cross-env NODE_OPTIONS=--experimental-vm-modules jest __development/tests/next-jest/path-alias-resolution.test.ts -t 'test1-2'
```
* `cross-env NODE_OPTIONS=--experimental-vm-modules` - Jest を ESM モードで実行する。

# Node:test

Jest を使用しないで Node の組み込みテストランナーを使用する場合の構成。テストコードを TypeScript / ESM 構文で実装し、 ESM モードでテストを実行する。

tsx をインストール

```bash
npm install -D tsx
```

パス解決の設定

```js
// tsconfig.json
{
  "compilerOptions": {
    ...
    "paths": {
      "@/*": ["./src/*"],
      "@/tests/*": ["./__development/tests/*"]	// ★これを追加
    }
  },
  ...
}
```
* 静的インポートも動的インポートも、tsconfig.json に基づいてパス解決される。

node:test を実行
```bash
npm exec -- node --test --import tsx [テストファイル名]

# コマンド例
# npm exec -- node --test --import tsx __development/tests/node-test/path-alias-resolution.test.ts
```
```bash
npm exec -- node --test --import tsx --test-name-pattern=[テストメソッド名] [テストファイル名]

# コマンド例
# npm exec -- node --test --import tsx --test-name-pattern='^test1-2$' __development/tests/node-test/path-alias-resolution.test.ts
```



# Prettier

インストール
```bash
npm install --save-dev prettier prettier-plugin-tailwindcss
```

設定ファイルを作成
``` bash
echo > .prettierrc.cjs
```
* .prettierrcだとJSON形式のためコメントが記述できない。コメントを記述できるように.jsファイルで作成し、かつCommonJS形式とする。

# ESLint

インストール
```bash
npm install --save-dev eslint-config-prettier
```

* `eslint-config-prettier` - Prettier と競合する ESLint の整形系ルールを無効化するプラグイン。  なお、ESLint に Prettierを組み込んで実行する（Prettier を ESLint ルールとして走らせる）やり方は現在は非推奨。Prettier はフォーマッタとして単体で動かし、ESLint は品質チェック、eslint-config-prettier で競合する ESLint の整形系ルールを無効化する。

設定ファイル

``` ts
// eslint.config.mjs
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";
import { defineConfig, globalIgnores } from "eslint/config";
import eslintConfigPrettier from "eslint-config-prettier"; // ★これを追加

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  globalIgnores([".next/**", "out/**", "build/**", "next-env.d.ts"]),
  eslintConfigPrettier, // ★これを追加。配列の最後に書くのがポイント
]);

export default eslintConfig;
```

* tailwindcssの存在しないクラスに対して警告を出すプラグイン(eslint-plugin-tailwindcss)は、Next.js(v16) & tailwind(v4)にはまだ対応されていない。

# エラースタックトレースの可読性向上

productionビルドにおけるコード圧縮（難読化）とTypeScriptのトランスパイルにより、実行時エラーのスタックトレースから元の関数名や .ts ファイル名が消失する。

例
```bash
stacks: 
Error:
    at h (/workspaces/portfolio-nextjs-2/my-app-1/.next/server/chunks/ssr/_02alokl._.js:1:451)
    at a.s.cause (/workspaces/portfolio-nextjs-2/my-app-1/.next/server/chunks/ssr/_02alokl._.js:1:715)
    at b (/workspaces/portfolio-nextjs-2/my-app-1/.next/server/chunks/ssr/[root-of-the-server]__13sj6sz._.js:1:2185)
    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)
    at async e (/workspaces/portfolio-nextjs-2/my-app-1/.next/server/chunks/ssr/_02alokl._.js:10:1608)
    at async g (/workspaces/portfolio-nextjs-2/my-app-1/.next/server/chunks/ssr/_0g0a8ar._.js:4:5559)
    at async f (/workspaces/portfolio-nextjs-2/my-app-1/.next/server/chunks/ssr/_02alokl._.js:10:1730)
    at async i (/workspaces/portfolio-nextjs-2/my-app-1/.next/server/chunks/ssr/_0g0a8ar._.js:4:6047)
    at async k (/workspaces/portfolio-nextjs-2/my-app-1/.next/server/chunks/ssr/[root-of-the-server]__13sj6sz._.js:1:2013)]]
```

エラースタックトレースの可読性を向上させるため、以下を設定する。

関数名の圧縮（難読化）の防止
```js
// next.config.ts
const nextConfig: NextConfig = {
    experimental: {
        serverMinification: false,  // ★これを追加。
        turbopackMinify: false,     // ★これを追加。
    },
};
```

ソースマップサポートの導入
```bash
npm install source-map-support
npm install -D @types/source-map-support
```

```ts
// アプリケーションの初期化部で適用する
// logging.winston.ts
import sourceMapSupport from 'source-map-support';
sourceMapSupport.install({
    environment: 'node',
    hookRequire: false,
});
```

導入後

例
```bash
stacks: 
Error:
    at customError (/workspaces/portfolio-nextjs-2/my-app-1/src/presentation/_system/error/error.factories.ts:161:18)
    at applicationError (/workspaces/portfolio-nextjs-2/my-app-1/src/presentation/_system/error/error.factories.ts:217:18)
    at _ (/workspaces/portfolio-nextjs-2/my-app-1/.next/server/chunks/ssr/[root-of-the-server]__13sj6sz._.js:145:86)
    at processTicksAndRejections (node:internal/process/task_queues:103:5)
    at withAuthAsync (/workspaces/portfolio-nextjs-2/my-app-1/src/presentation/_system/aspect/internal/aspect.auth.ts:25:12)
    at withErrorHandlingAsync (/workspaces/portfolio-nextjs-2/my-app-1/src/presentation/_system/aspect/internal/aspect.error-handling.server.ts:42:16)
    at withLoggingAsync (/workspaces/portfolio-nextjs-2/my-app-1/src/presentation/_system/aspect/internal/aspect.logging.ts:33:16)
    at withAdviceAsync (/workspaces/portfolio-nextjs-2/my-app-1/src/presentation/_system/aspect/aspect.server.ts:33:12)
    at ErrTestPage (/workspaces/portfolio-nextjs-2/my-app-1/.next/server/chunks/ssr/[root-of-the-server]__13sj6sz._.js:136:12)]]
```

# その他

TypeScriptの型チェック（コンパイル対象）から除外するフォルダを指定する。「__development/_bk」フォルダを除外
```js
// tsconfig.json
{
    "compilerOptions": {
        ・・・
    },
    "include": [
        ・・・
    ],
    "exclude": [
        "node_modules", 
        "**/_bk/**"	// ★これを追加
    ]
}
```
